<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Appointment</title>

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="bg-light">
    <div class="container">
        <h2 class="text-center mt-4">Edit Appointment</h2>
        <form action="/appointments/update/<%= appointment._id %>" method="POST" class="p-4 bg-white rounded shadow-sm w-50 mx-auto">
            
            <label for="name" class="form-label">Name</label>
            <input type="text" name="name" id="name" class="form-control mb-3" value="<%= appointment.name %>" required>
        
            <label for="date" class="form-label">Date</label>
            <input type="date" name="date" id="date" class="form-control mb-3" value="<%= new Date(appointment.date).toISOString().split('T')[0] %>" required>
        
            <label for="time" class="form-label">Time</label>
            <input type="time" name="time" id="time" class="form-control mb-3" value="<%= appointment.time %>" required>
        
            <div class="d-flex justify-content-between">
                <button type="submit" class="btn btn-success w-48">Update</button>
                <a href="/" class="btn btn-secondary w-48">Cancel</a>
            </div>
        </form>
    </div>
</body>
</html>
